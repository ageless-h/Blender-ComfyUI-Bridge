# This file is intentionally left blank for diagnostic purposes.

# Manifest for the "ComfyUI-BlenderBridge" addon.
# This file is for packaging and distribution, and complements the `bl_info` in `__init__.py`.
# For more information on Blender add-on manifests, see:
# https://wiki.blender.org/wiki/Reference/Release_Notes/4.2/Python_API#Add-on_Manifests

# This is the final, complete manifest based on official documentation and project requirements.
schema_version = "1.0.0"

id = "blender_comfyui_bridge"
version = "2.1.0"
name = "Blender-ComfyUI-Bridge"
tagline = "Blender and ComfyUI integration through ZMQ, HTTP and SSH"
maintainer = "Your Name"
type = "add-on"

authors = ["Your Name Here"]
# The category in the Add-ons listing.
category = "Render"
# A support level for this add-on.
support = "COMMUNITY"
# A URL for the add-on's documentation.
documentation_url = ""
# A URL for the add-on's bug tracker.
tracker_url = ""
# A list of other add-ons that this one depends on.
dependencies = []
# A list of Blender versions that this add-on supports.
blender_version_min = "4.0.0"
blender_version_max = "5.0.0"

# License conforming to https://spdx.org/licenses/ (use "SPDX:" prefix)
license = ["SPDX:GPL-3.0-or-later"]

# Optional fields
description = """
A robust bridge for two-way communication between Blender and ComfyUI,
enabling seamless render-to-texture workflows, with built-in SSH tunneling.
"""
tags = ["Render", "Pipeline", "Node", "ComfyUI", "SSH"]

# All Python dependencies are now handled by an internal script using pip.
# The 'wheels' directory is no longer needed.
# The following sections are intentionally left empty.
[python]

# Declare the permissions the add-on needs.
[permissions]
network = "Required to connect to the ComfyUI server and download dependencies on first run"
files = "Required to read rendered images and receive results"
